# Hello CLI

## Overview
- 터미널에서 간단한 인사 메시지를 출력하는 Node.js CLI 앱
- 기술 스택: Node.js (>=18), TypeScript (strict), Commander.js
- 검증: `npm run build && node dist/index.js greet World`

## Feature Notes (append-only)
